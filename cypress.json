{
  "baseUrl": "http://localhost:${PORT}",
  "env": {
    "key": "${CYPRESS_RECORD_KEY}",
    "codeCoverage": {
      "url": "http://localhost:${PORT}/coverage"
    }
  },
  "nodeVersion": "system",
  "projectId": "${CYPRESS_PROJECT_ID}",
  "video": true,
  "viewportHeight": 1080,
  "viewportWidth": 1920
}
